🌊 W9 — کاتالوگ مرکزی مدل‌ها + providerهای چندگانه

نکته: ai_solver.py و tests داخل این ZIP شامل تغییرات W8-v2
(Retry-After + لاگ تشخیصی ۴۲۹) هم هستند؛ اگر v2 را اعمال
نکرده بودید، با همین فایل‌ها هر دو موج را یک‌جا می‌گیرید.

فایل‌های جدید (مستقیم کپی شوند):
  tests/test_w9_providers.py

۹ فایل دیگر با مسیر اصلی ریپو داخل ZIP هستند. اگر ریپوی شما
W8 را مرج‌شده دارد (که طبق لاگ پرودی دارد)، extract مستقیم
این فایل‌ها امن است — پایه‌ی همه‌شان همان فایل‌های W8 است.
